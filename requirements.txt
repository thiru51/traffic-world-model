# Plain pip install path. The pixi.toml/pixi.lock pair is the reproducible one; this
# file exists so `python -m venv` works for people who do not want another tool.
#
# torch must come from PyTorch's own index or you get the CPU-only wheel:
#   pip install -r requirements.txt --extra-index-url https://download.pytorch.org/whl/cu124
#
# Versions are what was actually installed and measured on the RTX 4080 Laptop run
# recorded in results/. cu124 wheels work on any driver new enough for CUDA 12.x.

torch==2.6.0
metadrive-simulator==0.4.3
numpy==1.26.4          # MetaDrive 0.4.x is not numpy 2 clean, so this stays pinned <2
gymnasium==1.3.0
pygame==2.6.1
opencv-python==4.11.0.86
panda3d==1.10.13
shapely==2.1.2
scipy==1.17.1
pyyaml==6.0.3
matplotlib==3.11.1
imageio==2.37.4
tqdm==4.70.0
